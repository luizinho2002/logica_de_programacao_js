// Declara três variáveis para armazenar as idades
const idadeLuiz = 21;
const idadeAna = 14;
const idadeJulia = 8;

// Declara um array para armazenar as idades
const idades = [21, 14, 8, 21, 14, 8, 21, 14, 8, 21, 14, 8, 21, 14, 8];

/* 
    Inicializa um loop for para imprimir as idades na tela

     Esse loop vai de 0 até o comprimento do array, que é de 16. Em cada iteração do loop, o código imprime
     na tela a idade armazenada no índice [i] do array
 */
for(let i = 0; i < idades.length; i++) {
    console.log('resultado', idades[i]);
}


